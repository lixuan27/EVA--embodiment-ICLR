\section{Problem Formulation}
\label{sec:method}

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance=4mm and 5mm,
  box/.style={draw, rounded corners=2pt, align=center, font=\scriptsize, inner sep=3pt, minimum height=8mm},
  chan/.style={box, fill=gray!8, minimum width=30mm},
  ourbox/.style={box, fill=ourrow, minimum width=30mm},
  arr/.style={-{Stealth[length=2mm]}, thick},
]
\node[chan] (c1) {protocol\\target-absent vs target-seen\\(Section~\ref{sec:setting}, RQ4)};
\node[chan, right=of c1] (c2) {representation\\base $\beta$ / tool $\upsilon$ / camera $\mathbf{E}$\\(Section~\ref{sec:frames}, RQ1)};
\node[chan, right=of c2] (c3) {source diversity\\1 / 8 / 128 embodiments\\(Section~\ref{sec:data}, RQ2)};
\node[chan, right=of c3] (c4) {supervision\\language / subgoal / box / effect\\(Section~\ref{sec:aux}, RQ3)};
\node[box, below=7mm of $(c2)!0.5!(c3)$, fill=white, minimum width=70mm] (pol) {one backbone, one 48K-trajectory budget, one evaluation:\\3 tasks $\times$ 8 held-out targets $\times$ 100 rollouts $\times$ 3 seeds};
\draw[arr] (c1) -- (pol);
\draw[arr] (c2) -- (pol);
\draw[arr] (c3) -- (pol);
\draw[arr] (c4) -- (pol);
\node[ourbox, below=7mm of pol, minimum width=70mm] (eff) {camera-frame effect: anchor $\alpha_t = \Pi_c^t(T_t\kappa_e)$ from the kinematics of the robot in hand,\\displacement $\epsilon_t$ predicted by the policy, command decoded by Eq.~\eqref{eq:decode} on the target};
\draw[arr] (pol) -- (eff);
\end{tikzpicture}
\caption{Four channels through which source robots leave their mark on a policy, each isolated by one research question under a single backbone, budget and evaluation. The camera-frame effect representation sits on the representation channel and on the supervision channel and confines embodiment dependence to analytic anchoring and decoding.}
\label{fig:overview}
\end{figure}

\subsection{Embodiments, demonstrations and the two protocols}
\label{sec:setting}

An embodiment $e$ is a fixed-base arm with a two-finger gripper. Its configuration at time $t$ is the joint vector $q_t$ together with the gripper aperture $g_t$, and its forward kinematics $T_e$ maps $q_t$ to the tool pose $T_t = T_e(q_t) \in SE(3)$ expressed in the robot base frame $\mathcal{B}_e$. A demonstration on $e$ is a sequence of observations $o_t = (I^{\mathrm{s}}_t, I^{\mathrm{w}}_t, \ell)$, where $I^{\mathrm{s}}_t$ is the scene image, $I^{\mathrm{w}}_t$ is the wrist image and $\ell$ is the language instruction, paired with the motion of the tool. We write $\mathcal{D}(\mathcal{E}, \mathcal{T})$ for the demonstrations of a set of embodiments $\mathcal{E}$ on a set of tasks $\mathcal{T}$.

A policy is trained in two stages. Pretraining uses $\mathcal{D}(\mathcal{E}_{\mathrm{pre}}, \mathcal{T}_{\mathrm{pre}})$ over a source family $\mathcal{E}_{\mathrm{pre}}$. Post-training uses $\mathcal{D}(\{e_{\mathrm{s}}\}, \mathcal{T}_{\mathrm{post}})$ on a single source robot $e_{\mathrm{s}}$ with $\mathcal{T}_{\mathrm{pre}} \cap \mathcal{T}_{\mathrm{post}} = \varnothing$. Evaluation places the policy on a target embodiment $e_{\mathrm{h}} \neq e_{\mathrm{s}}$ for the same downstream tasks $\mathcal{T}_{\mathrm{post}}$. Two protocols differ in one condition. Under the \emph{target-absent} protocol the target satisfies $e_{\mathrm{h}} \notin \mathcal{E}_{\mathrm{pre}}$, so every parameter of the policy was fitted on other robots. Under the \emph{target-seen} protocol the target satisfies $e_{\mathrm{h}} \in \mathcal{E}_{\mathrm{pre}}$ while its downstream demonstrations stay excluded from all training. Both protocols are zero-shot with respect to the downstream data of the target, and they differ in whether the policy has seen the target at all. The study reports them separately throughout, and Section~\ref{sec:exp-exposure} measures the distance between them.

Embodiment changes are grouped into four categories by what differs between $e_{\mathrm{s}}$ and $e_{\mathrm{h}}$. Appearance changes alter the visual appearance while preserving morphology and kinematics. Gripper changes alter the end-effector geometry while keeping the arm. Arm changes alter the arm morphology or kinematics while keeping the end effector. Full changes alter both. The categories order the amount of source-specific structure that a policy has to leave behind, and every result is reported per category.

\subsection{Source specificity as the organising principle}
\label{sec:principle}

A policy transfers to $e_{\mathrm{h}}$ to the extent that the quantities it reads and writes carry nothing that belongs to $e_{\mathrm{s}}$ alone. Four channels decide how much source-specific structure enters. The representation channel decides the frame of the state and of the action. The data channel decides how many distinct source embodiments the policy averages over, which is the extent to which source-specific structure can be marginalised. The supervision channel decides which additional structure the policy is pushed to represent, which can be task structure shared by all robots or motion structure tied to the source. The exposure channel decides whether the target itself contributes to pretraining. Sections~\ref{sec:frames} to \ref{sec:aux} define the alternatives studied on each channel, and Section~\ref{sec:exp} measures each under the benchmark of Section~\ref{sec:bench}.

\section{Study Design}
\label{sec:design}

\subsection{Representation channel: three frames for the same motion}
\label{sec:frames}

Joint-space interfaces share neither dimensionality nor limits nor semantics across robots, so the study restricts itself to fixed-dimensional Cartesian interfaces of the tool and varies the frame in which they are expressed. The base-frame increment
\begin{equation}
\beta_t = \mathrm{vec}(T_{t+1}) - \mathrm{vec}(T_t), \qquad \mathrm{vec}(T) = [\,p,\ \mathrm{Euler}(R)\,] \in \mathbb{R}^6,
\end{equation}
measures the motion against $\mathcal{B}_e$, which each manufacturer places differently and which a target robot redefines. The tool-frame increment
\begin{equation}
\upsilon_t = \mathrm{vec}\!\left(T_t^{-1}\,T_{t+1}\right)
\end{equation}
measures the motion against the gripper itself, a frame that every two-finger robot carries in the same place relative to its fingers. The proprioceptive state admits the same choice: the absolute history $s^{\mathrm{abs}}_t = \{\mathrm{vec}(T_{t-i})\}_{i=0}^{h}$ exposes the base frame to the policy, and the relative history $s^{\mathrm{rel}}_t = \{\mathrm{vec}(T_t^{-1}T_{t-i})\}_{i=0}^{h}$ exposes only recent motion of the tool. A policy that drops proprioception altogether corresponds to the relative history with $h = 0$, since $T_t^{-1}T_t$ is the identity. The aperture $g$ is appended to every variant. The wrist camera moves rigidly with the gripper, so tool-frame quantities align with the motion the wrist image shows.

The tool frame still hides one fact from the policy. A new gripper places its fingertips at a different position relative to the tool origin, so the same $\upsilon_t$ produces a different contact outcome on the target. The third description moves the action into the camera frame, the single frame that every embodiment in the study shares by construction, and expresses it through the motion of the gripper itself as seen in the images.

\paragraph{Camera-frame motion effects.}
Each gripper carries a set of $K$ keypoints $\kappa_e = \{\kappa_e^{(k)}\}_{k=1}^{K}$ fixed in its tool frame: the two fingertips, the four pad corners and the palm centre, so $K = 7$. Their positions follow from the tool pose, $x_t^{(k)} = T_t\,\kappa_e^{(k)}$. Camera $c \in \{\mathrm{s}, \mathrm{w}\}$ projects world points with $\Pi_c^{t}$, where the superscript records that the wrist camera pose depends on $t$. The \emph{anchor} of keypoint $k$ in camera $c$ is its current image position
\begin{equation}
\alpha_t^{(k,c)} = \Pi_c^{t}\!\left(T_t\,\kappa_e^{(k)}\right),
\label{eq:anchor}
\end{equation}
an analytic function of the state and of the known kinematics of whichever robot is being controlled. The \emph{motion effect} over a horizon $H$ is the displacement of every keypoint from its anchor, projected through the camera pose held at time $t$:
\begin{equation}
\epsilon_t^{(k,c)}(\tau) = \Pi_c^{t}\!\left(T_{t+\tau}\,\kappa_e^{(k)}\right) - \alpha_t^{(k,c)}, \qquad \tau = 1,\dots,H .
\label{eq:effect}
\end{equation}
The effect tensor $\mathbf{E}_t \in \mathbb{R}^{H \times K \times 2 \times 2}$ collects the displacements of all keypoints in both cameras, and a visibility mask $m_t \in \{0,1\}^{K \times 2}$ marks keypoints that fall inside the image and in front of the camera. Three properties place $\mathbf{E}_t$ at the embodiment-free end of the hierarchy. Its units are pixels of a camera whose placement is matched across robots, so its scale is shared. Its semantics are the outcome of the action in the world, so a target with longer fingers produces the same effect for a successful grasp while requiring a different $\upsilon_t$. Its origin is subtracted analytically, so the policy learns a displacement field in place of an absolute position that would encode the geometry of the current robot; Section~\ref{sec:exp-mechanism} quantifies how much of the prediction error this subtraction removes. The keypoints of a new target come from its gripper model and the anchors from its forward kinematics, so the representation absorbs the geometry of the target at the input and output boundaries and leaves the learned mapping free of embodiment identity.

\paragraph{Objective and readouts.}
All representations share one backbone, a vision-language model with a separate action expert coupled through attention \citep{pi2025pi05, black2024pi0}, and one conditional flow matching objective \citep{lipman2023flow}. Let $z_t$ denote the target sequence of a representation over a chunk of $H = 12$ steps: $z_t = (\beta_{t:t+H}, g_{t:t+H})$ or $z_t = (\upsilon_{t:t+H}, g_{t:t+H})$ for the kinematic representations, $z_t = (\mathbf{E}_t, g_{t:t+H})$ for the effect representation, and $z_t = (\upsilon_{t:t+H}, g_{t:t+H}, \mathbf{E}_t)$ for effect co-training, where the two parts share the action expert and its attention over the observation. A noise level $\lambda \sim \mathrm{Beta}(1.5, 1)$ and Gaussian noise $\eta$ define the interpolant $z_t^{\lambda} = \lambda\,\eta + (1-\lambda)\,z_t$, and the network $v_\theta$ regresses the velocity with a mask that suppresses invisible keypoints:
\begin{equation}
\mathcal{L}(\theta) = \mathbb{E}\Big[\, \big\| w_t \odot \big(v_\theta(z_t^{\lambda}, \lambda \mid o_t, s_t, \alpha_t) - (\eta - z_t)\big) \big\|^2 \,\Big],
\label{eq:loss}
\end{equation}
where $w_t$ equals one on command channels and equals $m_t$ broadcast over the horizon on effect channels. The anchors $\alpha_t$ enter as clean conditioning tokens placed after the noisy target tokens, so the expert reads the current image positions of the keypoints before it predicts where they move. The effect head is initialised at zero, which keeps the command channels at the accuracy of the plain policy at the start of training and lets the effect channels grow from the shared features.

At inference the policy samples $\hat z_t$ by integrating the learned velocity field from noise over ten Euler steps. The kinematic representations execute their increments through an operational-space controller. Effect co-training executes the same command channels and treats $\hat{\mathbf{E}}_t$ as a learned regulariser that shapes the shared features. The effect representation omits the command channel and decodes one from the predicted image motion. For each future step $\tau$ the decoded pose is the least-squares solution
\begin{equation}
\hat T_{t+\tau} = \arg\min_{T \in SE(3)} \sum_{k,c} m_t^{(k,c)} \,\Big\| \Pi_c^{t}\!\left(T\,\kappa_{e}^{(k)}\right) - \alpha_t^{(k,c)} - \hat\epsilon_t^{(k,c)}(\tau) \Big\|^2 ,
\label{eq:decode}
\end{equation}
a perspective-$n$-point problem with fourteen correspondences at most, initialised at $T_t$ and solved with three Gauss-Newton iterations in under a millisecond. The command follows as $\hat\upsilon_{t+\tau-1} = \mathrm{vec}(\hat T_{t+\tau-1}^{-1}\hat T_{t+\tau})$, and the aperture channel is executed as predicted. Equation~\eqref{eq:decode} uses the keypoints and camera of the robot being controlled, so a target with a new gripper receives commands that realise the predicted effect with its own fingers.

\subsection{Data channel: controlled source diversity under a fixed budget}
\label{sec:data}

The pretraining budget is fixed to 48K trajectories, and the number of source embodiments that share it is varied over 1, 8 and 128 members of the procedurally generated family of Section~\ref{sec:bench}. Under this control a larger family reduces the number of trajectories per embodiment, from 48K for a single source to 375 for the full family, so diversity and per-embodiment density are traded against each other and the study reports both ends of the trade. The downstream post-training data and the evaluation stay fixed across the sweep, so the sweep isolates the diversity of the source family from every other factor.

\subsection{Supervision channel: auxiliary targets}
\label{sec:aux}

Auxiliary targets are added during pretraining and post-training as query-answer pairs sampled alongside imitation examples, with the imitation branch supervised by Equation~\eqref{eq:loss} and the auxiliary answers supervised by next-token cross-entropy. Four targets span the range from kinematic to image-grounded structure. Language motion renders the next tool-frame increment as a sentence of translation and rotation clauses. Subgoal pose predicts the next task-relevant waypoint of the tool in the current tool frame. Task box predicts the image box of the instructed object in each view. Effect predicts the camera-frame motion of the gripper keypoints of Equation~\eqref{eq:effect} through the co-training readout of Section~\ref{sec:frames}. All four use the same visual input as the imitation policy, and the ordering of their gains in Section~\ref{sec:exp-aux} reflects how much each target says about the interaction in the frame that the target robot shares.

\subsection{Exposure channel: target-seen pretraining}
\label{sec:exposure-design}

The target-seen protocol replaces a controlled fraction of the source budget with pretraining trajectories of the target embodiment while keeping the total budget fixed and keeping the downstream demonstrations of the target excluded from all training. Fractions of 5, 30 and 100 percent of the target data pool are studied, and an oracle that post-trains the shared pretrained model on the downstream demonstrations of the target serves as an upper reference. Comparing the exposed policies with their target-absent counterparts measures the distance between the two protocols and the extent to which exposure compensates for a less transferable representation.
